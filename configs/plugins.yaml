submodules:
  - name: Weaviate
    path: plugins/weaviate
    docker: "docker compose up -d"
  - name: Ollama
    path: plugins/ollama
    branch: main
    docker: "docker compose up -d"
use_nvidia: true
